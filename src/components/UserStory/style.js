import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    name: {
        textAlign: 'center',
        fontSize: 11,
        fontWeight: '500'
    }
})

export default styles;