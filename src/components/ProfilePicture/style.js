import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        margin: 5,
        borderWidth: 3,
        borderRadius: 50,
        borderColor: '#833AB4'
    },
    image: {
        borderRadius: 50,
        borderWidth: 1,
        borderColor: 'white'
    }
})

export default styles;